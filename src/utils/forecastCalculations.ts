/** Compatibility re-export — source of truth is `@skillgrid/shared`. */
export {
  type ForecastScenario,
  type ForecastSkillBreakdown,
  type ForecastEmployeeRow,
  type ForecastCategoryBar,
  type ForecastKPIs,
  type ForecastResult,
  generateForecast,
  generateForecastWithPlans,
} from "@skillgrid/shared/utils";
