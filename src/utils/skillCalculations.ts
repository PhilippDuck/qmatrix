/** Compatibility re-export — source of truth is `@skillgrid/shared`. */
export {
  getScoreColor,
  getNextLevel,
  getLevelByValue,
  getRoleTargetForSkill,
  getMaxRoleTargetForSkill,
} from "@skillgrid/shared/utils";
