/** Compatibility re-export — source of truth is `@skillgrid/shared`. */
export {
  calculateHistoricalXP,
  type ComparisonPeriod,
  type PeriodBoundaries,
  getPeriodBoundaries,
  calculateAverageScore,
  calculatePercentageChange,
  calculateSkillCoverage,
  calculateGoalFulfillment,
  calculateAverageGap,
} from "@skillgrid/shared/utils";
